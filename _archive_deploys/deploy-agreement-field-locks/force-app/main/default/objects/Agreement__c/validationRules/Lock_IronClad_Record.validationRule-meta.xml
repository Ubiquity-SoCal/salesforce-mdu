<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Lock_IronClad_Record</fullName>
    <active>true</active>
    <description>Allows users to link an IronClad Record when blank but blocks changes once populated. System Administrator bypasses.</description>
    <errorConditionFormula>AND(
  ISCHANGED(IronClad_Record__c),
  NOT(ISBLANK(PRIORVALUE(IronClad_Record__c))),
  $Profile.Name &lt;&gt; "System Administrator"
)</errorConditionFormula>
    <errorDisplayField>IronClad_Record__c</errorDisplayField>
    <errorMessage>IronClad Record link is locked once populated. Contact a System Administrator to change it.</errorMessage>
</ValidationRule>
