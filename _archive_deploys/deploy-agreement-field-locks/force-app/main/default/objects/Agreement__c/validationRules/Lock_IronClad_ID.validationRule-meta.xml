<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Lock_IronClad_ID</fullName>
    <active>true</active>
    <description>Allows users to add an IronClad ID when blank but blocks changes once populated. System Administrator bypasses.</description>
    <errorConditionFormula>AND(
  ISCHANGED(IronClad_ID__c),
  NOT(ISBLANK(PRIORVALUE(IronClad_ID__c))),
  $Profile.Name &lt;&gt; "System Administrator"
)</errorConditionFormula>
    <errorDisplayField>IronClad_ID__c</errorDisplayField>
    <errorMessage>IronClad ID is locked once populated. Contact a System Administrator to change it.</errorMessage>
</ValidationRule>
