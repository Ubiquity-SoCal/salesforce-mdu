<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Recipient__c</fullName>
    <label>Recipient</label>
    <type>Lookup</type>
    <referenceTo>Outreach_Recipient__c</referenceTo>
    <relationshipLabel>Messages</relationshipLabel>
    <relationshipName>Messages</relationshipName>
    <deleteConstraint>Restrict</deleteConstraint>
    <required>true</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <description>Restrict on delete: removing a recipient row would silently unblock re-mailing that person, so it must not be possible while a message references it.</description>
</CustomField>
